version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - node --version
        - npm --version
        - npm ci
    build:
      commands:
        - npm run build
        - ls -la .next/
        - cp .next/required-server-files.json ./
        - cp .next/trace ./
        - ls -la required-server-files.json
        - ls -la trace
  artifacts:
    baseDirectory: .
    files:
      - .next/**/*
      - public/**/*
      - package.json
      - next.config.js
      - node_modules/**/*
      - required-server-files.json
      - trace
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 